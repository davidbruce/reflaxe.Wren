package wrencompiler.components;

#if (macro || wren_runtime)

import haxe.macro.Type;

import reflaxe.BaseCompiler;

import wrencompiler.WrenCompiler;

function compile(enumType: EnumType, options: EnumOptions) {
    return null;
}

#end
